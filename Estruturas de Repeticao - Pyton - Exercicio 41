# 41 - Faça um programa que calcula a associação em paralelo de dois resistores R1 e R2 fornecidos pelo
# usuário via teclado. O programa fica pedindo estes valores e calculando até que o usuário entre com um valor
# para resistencia igual a zero.
# R = (R1 * R2) / (R1 + R2)

r = 0

while True:
    r1 = float(input('Informe o valor do 1º resistor: '))

    if r1 == 0:
        break

    r2 = float(input('Informe o valor do 2º resistor: '))

    if r2 == 0:
        break

    else:
        r = (r1 * r2) / (r1 + r2)
        print(f'O valor de dois resistores associados em paralelo é {r}')
